version: '3.9'
services:
  web:
    build: .
    image: "dune"
    ports:
      - "80:8000"
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      DEBUG: True
  redis_cache:
    image: "redis:alpine"
    ports:
      - 6379:6379